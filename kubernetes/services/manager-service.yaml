apiVersion: v1
kind: Service
metadata:
  name: manager-service
  namespace: dena
spec:
  selector:
    app: manager-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP  
